wx.config({
    debug: false,
    appId: '<%= @app_id %>',
    timestamp: <%= @timestamp %>,
    nonceStr: 'Wm3WZYTPz0wzccnW',
    signature: "<%= @signature %>",
    jsApiList: [
    'chooseImage',
    'checkJsApi',
    'onMenuShareTimeline',
    'onMenuShareAppMessage',
    'getNetworkType'
    ]
});

wx.error(function (res) {
    // alert(res.errMsg);
});
